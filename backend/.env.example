# DreamParty Backend Environment Configuration

# Google Gemini API Key (Required for actual AI generations; fallback mock response used if empty)
GEMINI_API_KEY=

# Server Configuration (Optional)
PORT=8001
HOST=127.0.0.1
CORS_ORIGINS=["http://localhost:5173", "http://127.0.0.1:5173", "http://localhost:3000"]
